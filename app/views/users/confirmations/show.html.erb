<!DOCTYPE html>
<html>
  <head>
    <title>Confirmação de Conta</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <style>
      body {
        font-family: Arial, sans-serif;
        display: flex;
        align-items: center;
        justify-content: center;
        height: 100vh;
        background-color: #f5f5f5;
        margin: 0;
      }
      .container {
        text-align: center;
        background: #fff;
        padding: 2rem;
        box-shadow: 0 2px 6px rgba(0,0,0,0.1);
        border-radius: 8px;
      }
      .container h1 {
        margin-bottom: 1rem;
      }
      .container a {
        display: inline-block;
        margin-top: 1.5rem;
        padding: 0.5rem 1.2rem;
        background: #0052CC;
        color: #fff;
        text-decoration: none;
        border-radius: 4px;
      }
      .container a:hover {
        background: #003C99;
      }
    </style>
  </head>
  <body>
    <div class="container">
      <h1>Conta Confirmada com Sucesso!</h1>
      <p>Obrigado por confirmar seu e-mail. Agora você pode fazer login.</p>
      <a href="<%= root_path %>#/login">Ir para Login</a>
    </div>
  </body>
</html>
